# URL del Worker de Cloudflare que firma las subidas (sin barra final).
# Si está vacío, la app usa el adaptador local (IndexedDB) para la demo.
# Ejemplo: https://copyvending-uploads.tu-subdominio.workers.dev
VITE_UPLOAD_API=
